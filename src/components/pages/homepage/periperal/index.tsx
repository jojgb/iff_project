import { FunctionComponent } from "react";

interface PeriperalProps {
  className?: string;
}

const Periperal: FunctionComponent<PeriperalProps> = () => {
  return <div>Periperal</div>;
};

export default Periperal;
